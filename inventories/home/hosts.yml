---
all:
  hosts:
    orcus:
      ansible_host: orcus.local
      ansible_user: pi
    dione:
      ansible_host: dione.local
      ansible_user: pi
    mimas:
      ansible_host: mimas.local
      ansible_user: pi
    tethys:
      ansible_host: tethys.local
      ansible_user: pi
    enceladus:
      ansible_host: enceladus.local
      ansible_user: pi
  children:
    julia_hosts:
      hosts:
        mimas:
        tethys:
        dione:
        enceladus:
  # children:
  #   microservice_hosts:
  #     hosts:
  #       tethys:
  #   microservice_message_broker:
  #     hosts:
  #       titan:
