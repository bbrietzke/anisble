server {
    listen 80 ;
    server_name consul_ui;
    root {{ consul_data_dir }};
    location / {
        proxy_pass http://127.0.0.1:8500/ui;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   Host      $http_host;
    }
}