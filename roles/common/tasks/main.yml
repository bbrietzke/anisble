---
- name: ssh without passwords
  authorized_key:
    user: pi
    state: present
    key: https://github.com/bbrietzke.keys

- name: Update all packages
  apt:
    name: "*"
    state: latest

- name: Install Tools
  apt:
    name: "{{ p }}"
  vars:
    p:
      - screen
